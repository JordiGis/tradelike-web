# Not secret — client/slot ids are already public in the page HTML and
# ads.txt. Committed (unlike .env.local) so the CI build in
# .github/workflows/deploy.yml actually has them.
VITE_ADSENSE_CLIENT_ID=ca-pub-7822806427334867
VITE_ADSENSE_SLOT_ID=1267522198
